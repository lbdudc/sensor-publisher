###<@ return data.basicData.languages
.filter(function(lang) { return lang !== "es" && lang !== "gl"; })
.map(function(lang) {
return {
fileName: lang + '/components/change-base-layer.json',
context: lang
};
});
>###
###< if (feature.MV_LM_BaseLayerSelector) { >###
{
"changeBaseLayer":{
"changeBaseLayerTitle": "Change base layer",
"selectBaseLayer": "Select a new base layer"
}
}
###< } >###
