###<@ return data.basicData.languages
    .filter(function(lang) { return lang !== "es" && lang !== "gl"; })
    .map(function(lang) {
      return {
        fileName: lang + '/components/payment.json',
        context: lang
      };
    });
>###
###< if (feature.T_Payments) { >###
{
  "payment": {
    "name": "Payment",
    "prop": {
      "amount": "Amount",
      "name": "Name",
      "type": "Payment type"
    },
    "form": {
      "title": "Payment",
      "validation": {
        "amount_max": "The amount cannot be higher than {amount}{currency}",
        "amount_min": "The amount cannot be lower than {amount}{currency}",
        "amount_required": "Amount is mandatory",
        "method_required": "A payment method is required",
        "name_required": "Name is required"
      }
    },
    "error": {
      "payment_error": "An unexpected error happened while processing payment"###< if (feature.T_P_PayPal) { >###,
      "paypal_error": {
        "abort": "Transaction cancelled",
        "error": "An error occurred during transaction",
        "load_script": "Failure on PayPal button render, reload page and try again.",
        "title": "PayPal error"
      }
      ###< } >###
    },
    "messages": {
      "successfull_payment": {
        "title": "Payment completed",
        "text": "Payment completed successfully."
      },
    },
    "methods": {
    ###< if (feature.T_P_RedSys) { >###
      "CREDIT_CARD": "Credit card",
    ###< } >###
    ###< if (feature.T_P_PayPal) { >###
      "PAYPAL": "PayPal"
    ###< } >###
    },
    "redsys": {
      "error": "Payment failed",
      "home": "Home",
      "redirection_notification": "You will be redirected to home in {val} seconds, or access now by clicking the button below",
      "success": "Successfull payment"
    }
  }
}
###< } >###
