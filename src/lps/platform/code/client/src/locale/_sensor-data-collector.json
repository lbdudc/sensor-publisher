###<@ return data.basicData.languages
    .filter(function(lang) { return lang !== "es" && lang !== "gl"; })
    .map(function(lang) {
      return {
        fileName: lang + '/components/sensor-data-collector.json',
        context: lang
      };
    });
>###
###< if (feature.MWM_TA_SensorDataCollector) { >###
{
    "sensorDataCollector": {
        "employee": "Employee",
        "search": "Search",
        "today": "Today",
        "day": "Day",
        "week": "Week",
        "month": "Month",
        "all": "All",
        "activities": "Activities",
        "locations": "Locations",
        "segments": "Segments",
        "segmentId": "Segment ID",
        "startTime": "Start time",
        "endTime": "End time",
        "tag": "Tag",
        "title": {
            "format": "{month} {day} {year}"
        },
        "transform": {
            "button": "Transform",
            "information": "Click here to run annotation task",
            "call": {
                "success": "The task was ran successfully",
                "error": "There was a failure running the task"
            },
            "running": {
                "button": "Running"
            }
        },
        "startDateError": "The start date must be earlier than the end date",
        "selectOneEmployee": "You must select one employee"
}

}
###< } >###
